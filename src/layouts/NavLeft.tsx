import * as React from 'react';

interface INavLeftProps {
}

const NavLeft: React.FunctionComponent<INavLeftProps> = (props) => {
  return (
    <div className='left'>NavLeft组件</div>
  );
};

export default NavLeft;
